[project]
name = "impact-metrics-ledger-backend"
version = "0.1.0"
description = "Backend evidence engine for Impact Metrics Ledger"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
